# Flask web app
Flask==2.3.3
Werkzeug==2.3.7

# Deep learning (garbage_model.keras needs TF 2.16+ / Keras 3)
tensorflow>=2.16.0

# Numeric / image
numpy>=1.24.0
scipy>=1.9.0
Pillow>=10.0.0
scikit-image>=0.20.0

# Optional: TACO detection mode (needs numpy 1.x + imgaug)
pycocotools>=2.0.0
imgaug>=0.4.0

pandas>=1.5.0
matplotlib>=3.5.0
requests>=2.28.0
